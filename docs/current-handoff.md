# 当前项目交接

更新时间：2026-07-14

本文件用于新对话快速接续当前项目。开始修改前，必须同时阅读：

- `AGENTS.md`
- `docs/project-structure-rules.md`
- 本文件

## 1. 当前目标

项目正在把《富饶之城》原型整理为正式版结构。当前界面只保留四个阶段：

- 大厅
- 准备房间
- 对局
- 结算

首页不需要。当前优先级是继续稳定对局 UI、4 到 8 人布局和正式游戏流程；不要重新推翻已经确认的大厅与准备房间布局。

## 2. 工作区保护要求

- 正式玩法基础已在提交 `ab16d97` 保存；本轮交互可靠性、表现、V2 调参和回归系统仍需作为一个整体提交。
- 不要执行 `git reset --hard`、`git checkout --` 或批量回滚。
- 不要因为文件未跟踪就删除它们；当前新拆出的大厅、对局、结算模块和生成资源大多仍是未跟踪文件。
- 修改前先读取目标文件现状，在现有成果上继续迭代。
- 不要为普通 UI 调整重新启动大规模多 Agent 流程。

## 2.1 2026-07-13 正式版剩余任务实施状态

本轮已完成：

- 新建房间默认倒计时改为 45 秒，测试房仍可显式使用 15 秒。
- 对局金币、抽卡、选牌、建造、技能、建筑效果、墓地和结束回合统一增加 Socket 确认；前端提供“处理中”、5 秒网络超时解锁和明确失败提示。
- 日志与行动事件加入 `player / bot / timeout / offline / rule` 来源及自动原因；日志上限为 500，并可只看系统代操作。
- 空牌堆自动领金币、未选资源结束回合、超时选牌和离线推进均有公开安全提示，不泄露自动选择的私有卡名或身份。
- 表现移动与文字阅读已解耦：资源 1.2/2.8 秒、普通行动 1.4/3.0 秒、技能 2.2/3.6 秒、阶段提示 1.6/4.0 秒。
- 抽牌和技能目标使用统一不透明选择层；自己选择身份改为手牌上方的轻量浮窗，不使用全桌遮罩。两类倒计时均位于右上且无边框。
- `GameUiTuningConfig` 已升级 V2，支持资源图标、行动区、手牌轨道和对手牌堆；旧 V1 本地配置自动迁移。
- 4 人宽松、5–6 人标准、7–8 人紧凑及低高度安全预设已经加入组件间约束；低高度建筑、操作区、手牌与座位不再只靠单个滑块范围。
- 11 张以上手牌进入横向轨道，已验收到 40 张；对手隐藏手牌按真实数量生成叠层和精确数字，不加载正面卡图。
- 1365×668 的 4–8 人普通 / 满建筑座位矩阵通过；1893×881 同矩阵通过。
- 角色与建筑悬浮检视保持 50ms 关闭；对手小牌放大后的绝对尺寸与自己的手牌预览接近。
- 服务器新增卡牌守恒、规则数据覆盖和 4–8 人共 125 局固定种子模拟。
- 正式四人流程已改为“真人 + 3 名人机”；真实服务器停止、重启、恢复原回合并继续行动的冒烟检查通过。
- 快速 / 完整回归输出中文 `report.md`、结构化 `report.json` 和关键截图；所有临时页面及进程在成功或失败时都会清理。

当前发布阻塞：

- 正式卡面目录预期 76 项，当前为 0/76；程序卡面回退正常，但在正式资源补齐前不得发布 `1.0.0`。
- 功能快速回归最近一次完整通过报告位于 `output/regression/2026-07-12T19-13-52-215Z/`。
- 完整发布矩阵功能项已全部通过，最终报告位于 `output/regression/2026-07-12T20-22-59-438Z/`；报告中的 `releaseReady=false` 仅由正式美术 0/76 导致。

## 3. 已完成的界面结构

### 大厅与准备房间

- 大厅已按用户提供的参考图重做，保留创建房间、加入房间和房间号输入。
- 删除了首页、轻量测试模式和多余恢复房间提示。
- 左上玩家头像区域支持上传图片，不能用装饰层遮挡名称或头像入口。
- 准备房间已拆到 `client/src/pages/lobby`。
- 房间支持 4 到 8 人、添加人机、增加空座位和删除空座位。
- 房间设置入口已经强化为明确按钮，但增加 / 删除座位不放进设置弹窗。
- 明弃 / 暗弃规则会根据人数由共享规则判断是否可用；不可用时设置项变灰。
- 4 人时不强制出现滚动，5 到 8 人才允许座位区扩展或滚动。

### 对局界面

- 对局模块位于 `client/src/pages/game`，主容器是 `GameTableView.tsx`。
- 已拆分顶部栏、中央状态、对手座位、自己区域、自己城市、行动栏、目标提示和角落入口。
- 当前正式桌面背景使用 `client/public/assets/generated-ui/citadels-game-table-background-v2.png`。
- 中央行动提示格式为“编号号身份名 · 玩家名行动”；角色选择阶段不会提前泄露身份。
- 王冠根据服务端公开的 `crownPlayerId` 移动，使用透明无外框资源 `citadels-crown-icon-v1.png`。
- 自己和其他玩家的身份牌都采用卡牌形式；未公开身份只显示未公开状态。
- 对手隐藏手牌只展示数量和牌背，不发送或渲染私有牌面。
- 其他玩家已建建筑会完整正面展示名称、费用、颜色和说明浮窗。
- 最新对手布局样式最终覆盖放在 `client/src/styles/game-opponents.css`，该文件在 `game-table.css` 后加载。
- 玩家名片目前限制为适度宽度并增加高度，不再通过拉成长条实现放大。
- 身份牌、隐藏手牌和已建建筑牌均已放大，并拥有独立网格区域。
- 顶部玩家避开左上房间 / 目标信息面板；左右上下座位、中央状态、建筑区、行动栏与手牌区已重新分配安全间距。
- 7 人布局已经改为左右对称；8 人左右座位使用密集模式，顶部座位保持较大尺寸。
- 行动栏位于自己的手牌上方；自己已建建筑位于中央状态和行动栏之间。
- 获取金币、抽卡、技能按钮和身份技能都使用悬浮说明，不再打开技能二级页面。
- 抽牌二选一和身份选择都使用完整卡牌组件，为后续正式卡牌美术预留插图区域。

### 当前游戏流程

- 如果玩家没有领取金币或抽牌就结束回合，服务端会自动选择领取 2 金币。
- 抽牌候选未完成时会锁定建造、角色技能和建筑效果。
- 建造确认弹窗在确认、取消或回合结束后会关闭。
- 人机会正常进行资源选择和建造，不应只结束回合。
- 主动建筑技能通过点击自己已经建好的建筑牌触发。
- 需要弃手牌的建筑技能会进入本地选择状态，在手牌右侧显示确认和放弃；只有确认时才向服务端发送牌 ID。
- 隐藏信息统一通过 `server/src/game/visibility.ts` 生成玩家视角状态，不能直接广播服务端完整 `GameRoom`。

### 结算界面

- 正式结算模块位于 `client/src/pages/result`。
- 获胜者会展示玩家头像和名称。
- 排名展示建筑分、奖励分和总分。
- 每位玩家的已建建筑会在结算中完整公开展示。
- 4 到 8 人排名区域采用纵向滚动，单个玩家建筑轨道可以横向滚动。

## 4. 最新一次修改

用户指出：

- 玩家名片被错误拉成长条。
- 每位玩家卡牌区域仍然偏小。
- 左上信息面板与顶部玩家名片穿插。
- 其他座位可能存在潜在重叠。

已经完成：

- 名片改为约 248 到 268px 的受限宽度，并增加头像与名片高度。
- 普通对手建筑牌约为 56 x 82px；8 人侧边密集座位约为 52 x 76px。
- 身份牌和隐藏手牌同步放大。
- 左右座位改为“名片占整行，身份 / 手牌与建筑在下一行”的布局。
- 顶部三名玩家下移并均匀分布，避开左上信息面板。
- 左右上下座位增加垂直安全间距。
- 中央状态、自己建筑区、行动栏和手牌区重新分配垂直空间。

最新代表性验收：

- 视口：1893 x 881
- 人数：8 人
- 结果：通过，无座位、中央状态、行动栏、手牌区或左上信息面板重叠。
- 截图：系统临时目录 `zy-game-ui-layout-qa/1893x881-8-player-action.png`。

## 5. 验证状态

2026-07-11 最新检查结果：

- `npm.cmd run build`：通过。
- 客户端 TypeScript：通过。
- 服务端 TypeScript：通过。
- Shared TypeScript：通过。
- `git diff --check`：通过，仅有现存 LF / CRLF 提示。
- 8 人轻量 UI 碰撞检查：通过。

UI 验收脚本：`scripts/verify-game-ui-layout.mjs`。

### 2026-07-11 对手卡牌与身份选择补充

- 对手隐藏手牌固定为 2:3 纵向比例，并禁止 flex 布局压缩宽度；所有人数统一最多显示 3 张牌背，更多数量使用 `+N` 角标。
- 对手公开城市会按顶部座位和侧边座位的可用宽度，分别计算 4 到 8 张建筑的渐进叠放量；满 8 建筑仍留在自己的城市轨道内，不与手牌或相邻座位穿插。
- 8 人身份选择固定为单排卡牌；选择面板必须位于中央状态 / 倒计时下方、自己手牌上方。
- 1262 到 1500 宽度下，自己的区域使用左右等宽、中间手牌居中的三列结构，并与底部游戏日志 / 聊天入口保持间距。
- `scripts/verify-game-ui-layout.mjs` 新增 `roles` 和 `opponents` 验收模式；`opponents` 模式支持通过 `UI_QA_PLAYER_COUNTS` 选择人数，并能向每位对手注入满 8 建筑的布局夹具。
- 代表性验收已覆盖 1262 x 827 与 1893 x 881；4 到 8 人正常手牌和满 8 建筑矩阵全部通过，1262 x 827 的 8 张身份选择单排与倒计时避让也通过。

### 2026-07-11 牌桌目标选择补充

- 军阀不再自动选择第一名其他玩家和第一座建筑。点击“使用技能”后进入牌桌目标模式，所有合法的对手公开建筑高亮，不合法目标会变暗并在悬浮提示中说明原因。
- 点击目标建筑后使用现有确认弹窗，显示目标玩家、建筑名称和破坏费用；取消确认会回到牌桌继续改选，确认后才发送 `targetPlayerId` 与 `targetDistrictCardId`。
- 军阀目标模式提供“只领取收入”和“取消选择”，红色建筑收入会计入本次破坏的可用金币提示。
- 实验室继续直接在自己的手牌区选择弃牌；进入效果后所有手牌显示可选描边，选中后使用“确认弃置 / 取消选择”。没有新增不存在的建筑破坏效果。
- 服务端破坏规则抽到 `server/src/game/districtDestruction.ts`，统一处理保护、完整城市、要塞、长城加价、金币与墓地收回；服务端测试包含军阀破坏非相邻玩家建筑。
- `scripts/verify-game-ui-layout.mjs` 新增 `targeting` 模式，真实浏览器已验证军阀打开牌桌选择、选择非默认座位、取消后重选以及确认后退出。最新完整构建通过，服务端 89 项测试通过。

### 2026-07-11 技能目标与特殊建筑收口

- 刺客、盗贼不再预填目标身份。点击技能后会显示本局合法身份牌，先选中再确认；已行动身份、自己的身份，以及盗贼不能选择的刺客 / 被刺身份会从候选中排除。
- 魔术师先明确选择“弃牌并重抽”或“与玩家交换手牌”。弃牌模式直接在自己的手牌区多选；交换模式直接点击牌桌上任意其他玩家的名片，再通过确认弹窗提交。
- 技能目标状态集中在 `client/src/pages/game/roleSkillTargeting.ts`，与军阀的公开建筑目标状态分开，避免把交互判断堆回页面大组件。
- 魔法学校现在作为当前角色颜色计入国王、主教、商人和军阀收入；军阀选择目标时的可用金币提示也同步计入这笔待领取收入。
- 墓地不再自动强制扣除 1 金币。建筑被破坏后，在线真人持有者会收到“支付 1 金币收回 / 放弃”选择；选择完成前攻击者不能继续行动，机器人或离线玩家使用确定性的自动处理，行动超时则自动放弃。
- 天文台、图书馆、实验室、铁匠铺、鬼城五色计分、要塞、长城、墓地、魔法学校和高分建筑均已按现有卡牌数据完成规则复核；新增天文台 / 图书馆、魔法学校、墓地选择测试。
- `scripts/verify-game-ui-layout.mjs` 新增 `skills`、`skill-role` 和 `skill-discard` 模式。1893 x 881 下真实 8 人桌面的刺客选身份、魔术师多选手牌、魔术师点玩家交换均通过。
- 1893 x 881 下重新验收 4、5、6、7、8 人普通城市和每名对手满 8 建筑，共 10 个场景全部通过；8 人身份选择和军阀桌面目标也通过。
- 最新服务端测试为 93 项，4 到 8 人完整开局、行动、结算流程均通过；完整构建通过。

### 2026-07-11 轻量卡牌检视器

- 对局新增 `GameCardInspector.tsx` 与 `cardInspectorData.ts`，角色牌、自己的手牌、抽牌候选、自己已建建筑和其他玩家公开建筑统一支持鼠标悬停大卡预览。
- 普通检视器采用轻量尺寸：桌面大屏卡面约 128–132px 宽、182–188px 高，说明板位于卡面下方；低高度视口会继续缩小，不使用早期接近全屏的大卡方案。
- 对手桌面原卡比自己的手牌小，因此不套用与自己手牌相同的放大倍数，而是按最终绝对尺寸对齐：1893×881 下自己的悬停手牌约为 97×139px，对手身份约为 98×140px，对手公开建筑约为 100×143px。这样不同来源的卡牌悬停后视觉大小基本一致。
- 自己的手牌不再复制一张完整大卡到桌面中央：悬停时原牌约放大 1.24 倍，紧邻手牌显示精简说明条；说明会按文字长度分档自动调整宽度和高度，短说明不再保留大空框，长说明会同时增加宽度和高度，并按手牌所在半区切换到牌组外侧，避免压住相邻手牌。
- 宽桌面的普通回合操作区已定稿为右下角操作台：金币 / 抽卡并排，使用技能与结束回合分别占满一行（2+1+1），面板增加纵向占比并与左侧自己的信息行保持底部对齐；身份选择、抽牌候选和技能目标等大型面板仍保持桌面居中，窄屏继续使用原居中布局。
- 检视器不接收鼠标事件，不会阻断建造、实验室选牌、军阀选建筑或身份目标点击；确认弹窗层级始终高于检视器。
- 鼠标进入后短暂稳定再显示，移开卡牌后固定 50ms 关闭；键盘聚焦卡牌也可以查看说明。
- 未公开身份只显示放大的牌背与“身份尚未公开”，不会读取或泄露真实角色；对手隐藏手牌仍然只有数量和牌背，不生成虚假牌面。
- `scripts/verify-game-ui-layout.mjs` 新增 `card-inspector` 模式，已通过 1893×881 与 1365×668 的 8 人桌面；覆盖角色、手牌、公开建筑、不同来源卡牌的最终绝对尺寸、短长说明框自适应、边缘定位、视口内显示和 50ms 关闭。

### 2026-07-11 最终桌面验收

- 1893×881 下 4、5、6、7、8 人普通城市和每名对手满 8 建筑共 10 个场景全部通过；满城卡牌留在各自轨道，不与隐藏手牌、身份牌或相邻玩家穿插。
- 1365×668 的低高度 8 人桌面通过；顶部、左右座位、中央倒计时、右下操作台、自己的手牌和底部入口均无交叠，角落牌堆文字不再挤压。
- 1262×827 的 8 张身份选择通过，中央倒计时始终可见，选择黑框位于倒计时下方且不压住自己的手牌。
- 军阀牌桌选建筑、刺客选身份、魔术师多选手牌弃置、魔术师点选其他玩家交换手牌均通过真实浏览器流程验收。
- 服务端 6 个测试文件共 93 项测试通过；完整客户端、服务端与 shared 构建通过；断线重连冒烟检查通过；`git diff --check` 通过，仅保留项目既有的 LF / CRLF 提示。

### 2026-07-12 右上角共享菜单

- 大厅、准备房间和对局右上角统一使用 `client/src/components/ui/UtilityMenuButton.tsx`，公告、帮助、设置沿用 `homepage-v1` 正式图标，不再由对局 CSS 临时绘制。
- 三张正式 PNG 是约 221×248 的竖向画布，统一通过 `object-fit: contain` 保持原始比例；不能同时拉满宽高导致横向变形。
- 新增 `client/public/assets/homepage-v1/icon-exit.svg`，使用与前三枚图标一致的黑色圆盘、双金边、上下纹章装饰和金色退出符号，替代原来格格不入的简化灰黑退出圆圈。
- 1893×881 下大厅、准备房间、8 人对局三页真实浏览器验收通过；公告、帮助、设置弹窗、键盘焦点、退出到大厅和重新创建准备房间流程均通过。
- `scripts/verify-game-ui-layout.mjs` 新增 `utility-menu` 模式，会检查三页按钮数量、图标原始比例、弹窗行为和键盘焦点，并保留三页截图。

### 2026-07-12 关键技能桌面演出

- 已新增 `client/src/pages/game/GameSkillPresentationLayer.tsx` 与 `client/src/styles/game-skill-presentations.css`。演出层绝对定位覆盖整个牌桌、保持 `pointer-events: none`，根据玩家标签的实时位置计算起点与目标点，不参与座位网格、手牌区域或玩家标签尺寸，因此后续可以继续做 UI 比例调音台。
- 共享层新增 `ActionEventPresentation` 结构，当前包含刺客指定 / 跳过、盗贼指定 / 实际偷取、魔术师换牌 / 弃牌重抽、军阀破坏建筑所需的安全字段。`GameLog` 和 `ActionEventPayload` 可以携带该结构，客户端不从中文消息反向解析数据。
- 服务端动作广播从“只取本次操作最后一条日志”改为按产生顺序广播本次操作的全部新日志，解决军阀破坏结果可能被后续红色收入日志覆盖的问题；人机完整回合产生的动作事件也会广播。
- 刺客现在分成两段演出：使用技能时显示从刺客玩家到目标身份的红色轨迹和身份封印；轮到目标身份时显示“该身份被刺客跳过行动”。公开演出只关联身份，不提前公开玩家；如果本地玩家自己是被跳过身份，客户端会基于自己的可见身份额外显示“你被刺杀”的私有红色警示。
- 盗贼同样分成两段：指定身份时显示黄色钱袋封印；实际结算时金币从被偷玩家标签飞向盗贼标签，并显示双方和准确金币数。
- 魔术师与玩家交换手牌时，两组牌背沿双向路径互换并显示交换前张数；弃牌重抽时显示紫色魔法路径和重抽张数。演出只公开数量，不公开具体手牌。
- 军阀破坏时显示从军阀玩家到目标玩家的战争轨迹、双方光圈、目标建筑幻影破裂，以及建筑名和实际花费。墓地后续选择仍沿用现有规则，不由演出层裁定。
- 正式美术粒子、角色专属音效和更高级的碎片素材尚未制作；当前是可直接游玩的 CSS 基础演出版，后续可以在不改变事件结构和布局的前提下继续精修。
- `scripts/verify-game-ui-layout.mjs` 已扩展技能演出检查：`skills` 覆盖刺客、魔术师两种方式，`targeting` 覆盖军阀破坏，新增 `skill-thief` 覆盖盗贼指定身份与实际金币转移；检查覆盖层占满牌桌、绝对定位且不接管点击，并保留演出截图。
- 2026-07-12 最新验证：服务端 6 个测试文件共 94 项通过，完整客户端 / 服务端 / shared 构建通过；1893×881 下刺客完整跳过链路、盗贼两段链路、魔术师换牌 / 重抽和军阀破坏的真实浏览器验收全部通过。

局部复验命令：

```powershell
$env:UI_QA_VIEWPORTS='1893x881'
$env:UI_QA_MODE='skill-role'  # 刺客指定与实际跳过
node scripts/verify-game-ui-layout.mjs

$env:UI_QA_MODE='skill-thief' # 盗贼指定与实际偷取
node scripts/verify-game-ui-layout.mjs

$env:UI_QA_MODE='skills'      # 刺客与魔术师
node scripts/verify-game-ui-layout.mjs

$env:UI_QA_MODE='targeting'   # 军阀选建筑与破坏演出
node scripts/verify-game-ui-layout.mjs
```

局部对手布局调整优先运行轻量模式：

```powershell
$env:UI_QA_MODE='dense'
$env:UI_QA_VIEWPORTS='1893x881'
node scripts/verify-game-ui-layout.mjs
```

完整多视口矩阵只在阶段性定稿时运行，避免每次局部调整都消耗大量时间。

## 6. 后续建议

下一次对话可以从以下事项继续：

1. 当前对局的防重叠、悬停检视器、右下操作台与 4 到 8 人位置布局已经完成阶段性验收；用户最新反馈是大致位置没有问题，但手牌区域、玩家标签等视觉比例仍难以匹配个人构想。后续不要整体推翻位置布局，优先按下方“UI 比例调音台”任务序列处理。
2. 后续正式卡牌美术生成或导入时，替换卡牌插图区，不改变当前卡牌容器比例与布局逻辑。
3. 后续如果引入新的角色或建筑牌，沿用当前显式目标选择与可见状态规则，不恢复默认目标。
4. 下一阶段可做正式版目录收口与无用测试页面清理，但必须先区分用户成果与真正废弃文件，不能批量删除未跟踪文件。

### 下一窗口任务序列：对局 UI 比例调音台

目标：保留已经通过验收的座位位置、安全区域和 4 到 8 人防重叠逻辑，把“组件放在哪里”与“组件看起来多大”分开，让用户可以直接调整手牌、玩家标签等视觉比例，再将满意参数固化为正式样式。

#### 第一阶段：先做可用的实时调参工具

1. 读取 `GameTableView.tsx`、`GameSelfArea.tsx`、`GamePlayerMiniStatus.tsx`、`GameOpponentSeat.tsx`、`game-table.css` 和 `game-opponents.css`，确认最终生效规则；不要先重写整个 CSS。
2. 在 `client/src/pages/game` 内新增开发专用的比例调节面板及配置模块，不把调参逻辑堆回 `GameTableView.tsx`。
3. 第一版只开放高影响参数：
   - 自己手牌的卡牌尺寸；
   - 手牌重叠量和手牌区域总宽度；
   - 玩家标签宽度、高度；
   - 玩家头像大小；
   - 对手身份牌、隐藏手牌和公开建筑牌大小；
   - 自己区域、建筑区、行动栏与手牌之间的垂直间距。
4. 参数通过对局根容器上的语义化 CSS 变量生效；卡牌优先使用单一宽度加 `aspect-ratio` 保持比例，避免分别调整宽高导致变形。
5. 面板仅在开发 / UI 验收模式显示，不能进入正式玩家界面；参数暂存到 `localStorage`，并提供恢复默认值与复制 / 导出当前配置的入口。
6. 增加“显示区域边界”开关，用半透明轮廓标出玩家名片、手牌、建筑区和行动区，便于用户判断实际占比。

#### 第二阶段：用户确认比例后再固化

1. 将用户确认的参数整理成对局专用视觉变量文件或清晰的对局样式变量区，避免同一尺寸继续散落在多个媒体查询中。
2. 响应式只保留标准桌面、中等桌面、低高度紧凑桌面三类必要修正；标准桌面以用户确认的视觉比例为基准，其余档位主要负责防重叠。
3. 清理被新变量替代的重复尺寸覆盖，但不要顺带改游戏规则、交互流程、可见状态或座位位置。
4. 如果新增文件或归类方式改变长期项目结构，同步更新 `docs/project-structure-rules.md`。

#### 验收顺序

1. 先在一个代表性视口（优先 1893×881、8 人）确认调节面板、实时变化、恢复默认和本地保存正常。
2. 用户确认视觉比例后，再运行现有轻量 UI 碰撞检查。
3. 固化样式后运行客户端构建，并复验 1365×668 低高度桌面及 4 到 8 人关键场景。
4. 第一阶段不要提前做完整 CSS 重构；目标是先让用户能快速表达并确定想要的比例。

## 7. 工作方式约定

- 默认由主 Agent 直接读取、修改和验证。
- 普通 UI / CSS 调整不使用 subagent。
- 只有真正独立、耗时较长或需要隐藏信息 / 安全审查的任务才启用 subagent。
- 单次最多 1 到 2 个 subagent，且不能同时写同一文件。
- UI 修改采用：读取相关模块 -> 主 Agent 修改 -> 单个代表性视口验收 -> 构建检查。
- 不重复扫描已确认范围，不为了形式分工增加等待时间和 token 消耗。

## 8. 本地运行

- 客户端：`http://127.0.0.1:5173/`
- 服务端默认：`http://127.0.0.1:3000/`
- 用户有时会手动启动服务；开始验收前先检查端口，不要无条件重复启动或结束用户进程。

## 9. 2026-07-13 对局 UI V4、固定中央信息与服务端超时收口

- UI 调音台升级为 V4，只保存一套适用于 4–8 人的全局值；自己的区域不再按人数重复调三次，对手区域在渲染时按 `roomy / standard / dense` 自动派生密度，低高度安全压缩同样不写回原始配置。
- V3 首次迁移以当前打开的人数档位为全局基准，并把当时已压缩的对手尺寸还原为基础值；旧存储仍保留，V4 保存只写 `zy-game-ui-tuning-v4`。
- 开发环境可以从对局右上角“设置 → UI 布局调试 → 打开调音台”进入，不再需要记忆 `?uiTune=1`；网址参数仍保留为快捷方式，生产构建不显示入口。
- 新增卡牌预览比例、操作区向左位置等调节项；手牌安全计算结合当前手牌数、叠放值、左右底部区域和视口，自动加深叠放或切换横向浏览。
- 抽牌和技能身份目标使用统一选择层；自己选身份使用独立轻量浮窗。标题居中、倒计时位于右上角且无外框；测试夹具只替换卡牌容器，不得删除标题和倒计时。
- 中央标题、阶段、当前玩家和倒计时保持固定坐标；最多两条行动结果改为其下方的独立绝对提示槽，提示出现或消失不会再顶动固定信息。当前行动玩家名片使用不改变几何尺寸的柔和金色呼吸光。
- 对手座位按调音变量计算名片、私有牌和城市轨道，不再由末尾固定 CSS 覆盖；左下牌堆、自己名片和身份牌同样使用实际变量列宽。
- 身份选择、皇冠展示和角色行动由服务端根据 `turnTimer.deadlineAt` 主动登记截止任务；身份选择超时从当前合法身份中随机选择，只公开“系统已完成选择”。每个任务在执行前重新核对房间、阶段、玩家和截止时间，旧客户端的 `resolve_turn_timeout` 仅保留为兼容兜底。
- 身份选择层保持右上公共菜单可点击，异常状态下仍能打开帮助、设置或退出房间。
- 极端手牌不再使用“每人 40 张”克隆夹具。QA 把当前配置的全部建筑牌按座位轮流发完：基础 65 张牌时，4 人为 17/16/16/16，8 人为 9/8/8/8/8/8/8/8；未来扩展卡加入数据后自动使用新的最大牌池。
- 浏览器验收通过独立上下文禁用缓存，测试结束在 `finally` 关闭页面、上下文和本轮服务进程，避免残留预览窗口。

## 10. 2026-07-13 建造即时动画与职业浮窗

- 建造确认采用视觉先行：客户端记录实际手牌矩形和预计城市槽位，完整卡面约 1 秒飞向建筑区，同时发送原有 `build_district` 命令；金币、手牌和城市仍完全以服务端状态为准。
- 每张建筑牌使用独立事务。成功时权威建筑在飞行结束前隐藏，结束后正式落位并短暂发光；拒绝或 5 秒未确认时从当前位置退回原手牌并显示原因；迟到成功会从缓存起点补一段缩短入场。
- 建筑师连续建造按卡牌 ID 分开管理；滚动手牌使用当前实际可见矩形；对手建造从对应玩家的隐藏手牌堆飞向其城市，飞行末段才显露正面。
- `GameSkillPresentationLayer.tsx` 已排除 `build_district`，不再出现通用方块、路线或重复建筑幻影。减弱动效下不飞行，只保留权威落位和结果状态。
- 自己选择身份使用 `GameRoleSelectionDock.tsx`，固定在手牌上方；所有当前可选职业牌必须单排完整铺开，不使用横向滚动条。不再创建全桌黑色遮罩，右上公共菜单保持可点击。等待其他玩家时只显示中央等待信息，不公开具体身份。
- `scripts/verify-game-ui-layout.mjs` 新增建造成功、服务器拒绝退回和职业浮窗多视口检查；`scripts/run-regression.mjs --browser-only` 可只重跑 UI 专项，且仍会在 `finally` 清理本轮页面和服务。

## 11. 下一窗口继续：职业浮窗细化与本轮验收收口

用户要求在此处暂停，不要在当前窗口继续启动页面或修改交互。下一窗口从以下内容接续：

### 新补充的职业选择要求

1. 职业选择浮窗中的每张身份牌必须接入现有 `GameCardInspector`：鼠标指向身份牌时显示放大的身份牌，下方显示对应技能说明，移开后继续使用现有 50ms 关闭延迟。不得单独复制另一套预览组件。
2. 职业选择黑色浮窗的宽度必须随当前可选身份牌数量自适应。少量身份时收窄到刚好容纳卡牌、间距和内边距；身份较多时增宽并完整铺开所有身份牌，不使用横向滚动条。禁止继续固定为 790px / 930px 的大空框。
3. 浮窗仍位于自己手牌正上方、无全桌遮罩；职业牌优先单排，倒计时保持右上无边框，右上公告 / 帮助 / 设置 / 退出保持可点击；不能公开其他玩家选择的具体身份。

### 当前代码状态

- 已新增 `useBuildAnimationTransactions.ts` 和 `GameBuildAnimationLayer.tsx`，实现完整卡面视觉先行、权威成功提交、拒绝 / 超时退回、迟到成功补动画、对手牌堆起飞、按卡牌 ID 隔离事务；旧 `GameSkillPresentationLayer` 已排除 `build_district` 通用方块动画。
- `GameSelfArea.tsx` 已为手牌加入 `data-hand-card-id` 和建造中锁定；`GameSelfCity.tsx`、`GameOpponentSeat.tsx` 已加入到达前隐藏、目标占位和落位发光状态。
- `useGameCommandFeedback.ts` 的失败提示已携带 `commandKey` 与拒绝 / 超时来源，建造事务可以只响应自己的命令失败。
- 已新增 `GameRoleSelectionDock.tsx`，自己选身份时不再使用全屏 `GameSelectionOverlay`；抽牌、刺客和盗贼等技能目标仍使用原居中选择层。
- `GameRoleSelectionDock` 已给身份牌写入检视器属性，“悬浮放大卡面 + 完整技能说明”已通过真实鼠标专项验收；检视器按实际渲染高度定位，不压住源卡。
- 职业浮窗已删除固定宽度与横向滚动容器，按身份牌数量自适应并完整铺开当前全部身份牌。

### 已完成验收

- 全项目生产构建通过。
- 服务端 105 项测试通过，其中包含 4–8 人共 125 局固定种子模拟。
- 建造动画专项通过：成功建造、完整卡面起点、权威城市落点、动画结束清理、服务器拒绝后退回原手牌、失败原因提示均通过。报告：`output/regression/2026-07-13T09-47-41-757Z/`。
- 1893×881 职业浮窗、无全桌遮罩、右上退出可点击和服务端身份超时自动选择已通过。
- 所有本轮浏览器与服务进程已由回归脚本 `finally` 清理，没有保留预览窗口。

### 暂停时尚未完成的验收

- 1365×668 职业浮窗实际画面无重叠，但旧检查要求 12px 间距，实际约 9.6px；已把低高度安全阈值修正为 4px，尚未重新运行。
- 1365×668 与 1262×827 的倒计时实际无边框且位于右上，但自动检查因 1px 顶部阈值失败；CSS 已将 `top: -1px` 修正为 `top: 0`，尚未重新运行。
- 下一窗口完成“浮窗随身份数量自适应”和身份牌悬浮检视后，再执行：`node scripts/run-regression.mjs --ui=roles --browser-only`。
- 建造专项虽已通过成功与拒绝退回，仍建议在最终完整回归中补跑建筑师连续三次、横向滚动手牌、对手建造、5 秒网络超时 / 迟到成功及减弱动效截图。
- 正式美术资源仍为 0/76，程序卡面回退正常；该项继续阻止 `1.0.0`，不属于本轮交互代码失败。

## 12. 新对话无缝衔接清单（2026-07-13 暂停点）

以下清单是当前工作区的真实暂停点。P0 已完成；新对话必须先阅读本节，再从 P1 顺序继续。不要重新设计已经完成的建造流程、职业浮窗方向、V4 全局调音或服务器权威超时。

### P0：已完成（2026-07-13）

1. **职业牌悬浮检视**
   - 在 `GameRoleSelectionDock.tsx` 中复用现有 `GameCardInspector`，不要新增第二套预览组件。
   - 鼠标悬停每张可选身份牌时，显示统一绝对尺寸的放大身份牌，并在下方显示该身份的完整技能说明。
   - 保留现有移开 50ms 关闭、视口边缘自动换边和调音台“卡牌预览大小”参数。
   - 专项检查最右侧职业牌时，检视器定位仍以当前可见卡牌矩形为准，且不被浮窗自身边界裁切。

2. **职业选择浮窗内容自适应宽度**
   - 删除当前固定 790px / 930px 一类宽度规则。
   - 宽度按“可选身份牌总宽度 + 卡间距 + 左右内边距”计算；身份少时浮窗收窄，身份多时逐步增宽。
   - 所有身份牌必须完整铺开，不启用横向浏览，也不为了少量身份保留大面积空黑框。
   - 继续保持手牌正上方、优先单排、右上无框倒计时、无全桌遮罩、右上公共菜单可点击和隐藏身份不泄露。

3. **重跑职业浮窗多视口验收**
   - `1893×881`、`1365×668`、`1262×827` 的三身份收窄、八身份完整铺开与单排布局均已通过，所有视口均无职业牌滚动条。
   - 职业浮窗不再使用 790px / 930px 固定宽度；宽度按实际卡牌内容增长，并受视口安全上限约束。
   - 真实鼠标悬停最右侧身份牌可显示统一检视器；完整技能说明可见，预览按实际渲染高度定位，不压住源卡且不受浮窗边界裁切。
   - 右上无框倒计时、公共菜单可点击、隐藏身份不泄露与服务端身份超时均通过。
   - 通过报告：`output/regression/2026-07-13T11-02-06-339Z/`。

### P1：建造即时动画尚需补齐的边界验收

- 已通过：普通成功建造、完整卡面从手牌起飞、权威城市落点、动画完成清理、服务器拒绝后退回和失败原因提示。
- 尚需补跑并记录截图：
  - 建筑师连续建造三张，三个事务必须按卡牌 ID 独立完成。
  - 极端手牌通过加深叠放留在安全区时，从当前实际可见矩形起飞；不得恢复横向滚动。
  - 其他玩家建造时，从对应对手隐藏手牌堆飞向其城市。
  - 5 秒网络超时先回退，随后迟到成功时播放缩短入场并服从服务器状态。
  - 断线/重连期间没有永久锁牌、重复正式建筑或残留飞行副本。
  - 减弱动效模式不飞行，只保留落位与结果高亮。
- 这些都是表现层验收，不得修改服务器建造裁定，也不得让客户端提前扣金币或移动权威卡牌。

### P2：完整回归与正式版收口仍未完成

1. 在 P0、P1 完成后重新运行完整构建、服务器测试和浏览器快速回归，生成新的中文 `report.md`、`report.json` 与关键截图。
2. 完整回归仍需覆盖 4–8 人、真实卡池极端手牌、0/4/7/8 座城市、身份/抽牌/技能选择、断线重连、再来一局和服务器停止后恢复。
3. 极端手牌夹具必须按当前最大建筑牌池轮流发完：基础 65 张时 4 人为 17/16/16/16，8 人为 9/8/8/8/8/8/8/8；禁止恢复“每人 40 张”的不可能场景。未来加入高级版卡池后自动使用新的实际总数。
4. 所有浏览器验收继续复用单一后台页面，并在 `finally` 关闭本轮页面、浏览器上下文和服务进程；无论成功失败都不得留下递归预览窗口。
5. 正式美术资源仍是 0/76。程序卡面和缺图回退可继续开发验收，但在 9 张角色、65 张建筑和两类牌背齐全并通过资源检查前，不得发布 `1.0.0`。
6. 发布前还要确认生产配置、服务器重启后的房间恢复、完整四人真人+机器人冒烟流程、部署说明和最终结构文档均与当前实现一致。

### 当前工作区与已验证基线

- 当前工作区包含一批**尚未提交**的连续修改；它们属于本轮成果。新对话开始时先执行 `git status --short` 和 `git diff --check`，不得 reset、checkout 或覆盖这些修改。
- 当前新增核心文件：
  - `client/src/pages/game/useBuildAnimationTransactions.ts`
  - `client/src/pages/game/GameBuildAnimationLayer.tsx`
  - `client/src/pages/game/GameRoleSelectionDock.tsx`
  - `client/src/pages/game/GameSelectionOverlay.tsx`
  - `client/src/pages/game/GameUiTuningSettingsEntry.tsx`
- 已验证基线：完整生产构建通过；服务器 105 项测试通过，其中包含 4–8 人共 125 局固定种子模拟。
- 建造动画通过报告：`output/regression/2026-07-13T09-47-41-757Z/`。
- 1893×881 职业浮窗与服务端身份超时通过报告：`output/regression/2026-07-13T09-30-45-046Z/`。
- P0 多视口职业浮窗、全部身份牌铺开与悬浮检视通过报告：`output/regression/2026-07-13T11-02-06-339Z/`。

### 新对话建议的第一条执行指令

“读取 `docs/project-structure-rules.md` 和 `docs/current-handoff.md`，确认第 12 节 P0 已完成，再从 P1 的建造即时动画边界验收继续；不要重做职业浮窗、V4 调音或服务端建造规则。”

## 13. 2026-07-13 九职业演出、开场皇冠与牌桌布局收口

本节是第 12 节之后的最新实现状态；若与前文的横向浏览、中央多条提示或技能目标大黑框描述冲突，以本节为准。

### 已完成实现

- 九个职业均接入现有 `GameSkillPresentationLayer`，没有另建第二套演出架构。刺客、盗贼、魔术师、国王、主教、商人、建筑师、军阀和王后的主动或自动效果均有对应视觉反馈，并支持减少动态效果。
- `assassin_skip` 与 `thief_steal` 使用关键反馈队列：刺客受害座位出现斩击、暗场和跳过提示；盗贼按实际数量展示金币流向，零金币也会显示未偷到金币。关键反馈至少保留约 2 秒，不会被普通回合提示顶掉。
- 刺客和盗贼的身份目标选择统一改为紧凑单排栏：左侧分别显示“选择一名玩家刺杀”“选择一名玩家偷窃”及隐藏身份说明，中间身份牌单排铺开，确定和取消固定在右侧；不再使用大面积空黑框。
- 开局由服务端计时驱动约 7 秒连续演出：0–3 秒本局目标、3–5.7 秒皇冠轮转、5.7–7 秒飞向最终玩家并落位。刷新或重连按剩余时间进入正确阶段；中央不再重复显示皇冠归属小字，桌面皇冠增大并保留柔和光晕。
- 中央行动反馈收口为单条短句。固定状态区只显示“轮到 XXX 行动”，不再弹“行动结束”；抽牌只显示“XXX 抽取了卡牌”。普通提示可替换，职业技能、刺杀、盗窃、超时和终局提示保证展示；完整过程仍写入游戏日志。
- 自己手牌彻底取消横向滚动和滚动条。极端牌数通过动态加深重叠保留在手牌安全区，首张、中间张和末张均可悬停、点击和建造。
- 对手身份、隐藏手牌和公开建筑使用独立布局轨道。浏览器专项已按 4–8 人、普通城市和满城市两类场景逐个检查身份区、隐藏手牌、建筑区、头像、相邻座位与视口边缘；只允许同一手牌堆或同一城市内部的设计性重叠，跨区域至少保留 4px 间隔。

### 验收结果

- 最终组合专项（开场、职业浮窗、单条短提示、极端手牌）在 `1893×881`、`1365×668`、`1262×827` 全部通过：`output/regression/2026-07-13T13-23-13-552Z/`。
- 4–8 人跨区域矩形碰撞专项在三种目标视口、普通城市和满城市场景全部通过：`output/regression/2026-07-13T12-31-46-080Z/`。
- 九职业通用收益与自动效果专项通过：`output/regression/2026-07-13T13-03-34-500Z/`。
- 刺客跳过与盗贼偷窃关键结算专项通过：`output/regression/2026-07-13T13-13-54-020Z/`。
- 盗贼紧凑单排目标栏最终复验通过：`output/regression/2026-07-13T13-20-26-887Z/`。
- 服务端规则测试 106/106 通过，其中继续包含 4–8 人共 125 局固定种子模拟；客户端、服务端和共享模块生产构建全部通过。
- `git diff --check` 在最终交接更新后通过；仅有工作区既有的 LF/CRLF 转换提示，没有空白错误。

### 仍然有效的发布阻塞

- 正式美术资源仍为 0/76，因此回归报告中的 `releaseReady` 仍为 `false`。这不代表上述功能验收失败，但在 9 张角色、65 张建筑和两类牌背齐全并通过资源检查前不得发布 `1.0.0`。
- 本轮没有改变建造裁定、胜负规则、Socket 请求协议、V4 调音架构或项目模块归类；第 12 节 P1 中尚未完成的建造动画边界验收仍可后续独立继续。

## 14. 2026-07-13 皇冠演出、对手建造卡面与职业收入说明修正

本节覆盖第 13 节中“皇冠轮转”和第 12 节 P1 的对手建造卡面描述；后续继续时以本节为准。

### 已完成实现

- 开场仍使用服务端约 7 秒时序，但 3–5.7 秒期间皇冠固定在桌面中央，只按约 450ms 的节奏切换候选玩家座位光圈；5.7–7 秒仅进行一次从中央到最终玩家的平滑落位。原有三层快速扩散光环已改为单层慢速呼吸光。
- 刷新和重连会根据服务端剩余时间直接恢复当前候选光圈或皇冠落位进度，不会从头重播。减少动态效果模式直接高亮最终座位并淡入皇冠，不播放轮转与飞行动画。
- 对手建造动画现在从隐藏手牌堆最上方的真实卡背矩形起飞，不再从包含计数和留白的整组容器起飞。飞行卡始终使用固定卡牌比例和统一等比缩放，卡名、费用和卡面文字不会发生横向或纵向拉伸。
- 建造成功、服务端拒绝退回、退回过程中迟到成功以及减少动态效果均复用同一套等比几何。服务端权威状态、建造费用、正式建筑落位和 Socket 指令没有改变。
- 新增对局内部职业收入文案辅助 `client/src/pages/game/roleIncome.ts`。国王、主教、商人、军阀的身份牌检视、无障碍文本和服务器角色资料现在明确写出收入公式，并说明魔法学校计入当前职业颜色。
- 四个收入职业的技能按钮会显示“职业收入预计 +N”，悬浮说明给出颜色建筑、魔法学校和商人固定金币的实际计算过程；结算后显示“职业收入已结算”。零收入明确显示 0，商人没有绿色建筑时仍预计固定收入 1。
- 军阀目标选择区会同时显示预计收入，“只领取收入”按钮直接显示预计数量；选择破坏仍只有服务器确认成功后才结算收入，客户端不会提前增加金币。

### 验收结果

- 皇冠中央停留、座位光圈、单次落位、重连恢复与减少动态效果专项通过：`output/regression/2026-07-13T14-00-32-395Z/`。
- 建造动画专项通过，覆盖本人成功、服务端拒绝退回、4 人对手建造、8 人密集侧边座位和减少动态效果；逐帧矩阵确认横纵缩放一致，卡背起点来自真实最上方卡背，减少动态效果下无移动副本或残留动画：`output/regression/2026-07-13T14-31-01-940Z/`。
- 职业收入专项通过，覆盖国王、主教、商人、军阀的普通颜色建筑、魔法学校、零匹配建筑、商人固定金币、技能演出和实际金币；另确认其他五个职业不显示无关收入：`output/regression/2026-07-13T14-22-23-020Z/`。
- 服务端规则测试 106/106 通过，完整生产构建通过；`git diff --check` 通过，仅有既有 LF/CRLF 转换提示。

### 仍然有效的边界

- 正式美术仍为 0/76，是当前唯一已知的 `1.0.0` 发布阻塞；上述三个功能专项均为通过状态。
- 本节没有改变职业收入数值、建造裁定、胜负规则、共享协议或项目模块归类，因此无需修改 `docs/project-structure-rules.md`。

## 15. 2026-07-13 身份叫号揭示与窄窗口牌桌重排

本节是当前最新实现状态；后续处理对局轮替或紧凑布局时，以本节为准。

### 已完成实现

- 身份选择结束后由服务端进入权威 `ROLE_CALL` 阶段，并按启用职业编号依次执行“叫号 → 无人应答/身份揭示 → 行动”。叫号为约 650ms，无人应答约 550ms，身份揭示约 1350ms；只有揭示完成后才创建该玩家的 `ROLE_ACTION` 倒计时。
- 4–7 人只叫 1–8 号职业，8 人额外叫 9 号王后。未被玩家选择的职业也会正常叫号并显示“无人应答”。刺客目标在被叫到时进入独立 `skipped` 揭示，先完成斩击、封印和跳过反馈，再继续下一身份，不会启动受害玩家的行动倒计时。
- `VisibleGameState` 不下发服务端 `calledRoleIds`；`calling` 和 `unanswered` 阶段只公开职业编号与名称，`revealing` 或 `skipped` 后才公开对应玩家。未来身份、暗弃牌和未选择职业不会因此泄露。
- 活跃房间快照升级为版本 2。版本 1 的旧 `ROLE_ACTION` 房间会根据已完成身份和当前行动身份推导已叫号范围，恢复后不重播此前身份，也不丢失当前行动。
- 新增 `GameRoleCallSequence`，复用 `RoleIdentityCard`：叫号显示卡背、编号和职业名；响应时只翻开一次身份牌并高亮对应座位；无人响应时灰化并盖“无人应答”；刺杀跳过使用独立封印状态。减少动态效果模式改为淡入，但保持服务端时序不变。
- 行动阶段的中央状态区持续显示小型正面身份牌、职业编号、玩家名和剩余倒计时，不再只依赖短暂的“轮到玩家”文字。
- 当宽度不超过 1100px，或宽度不超过 1365px 且高度不超过 640px 时启用紧凑牌桌。牌桌使用真实视口，不再依赖 920px 最小桌宽或页面滚动；对手席位改为顶部最多四列的一至两行网格，末行居中。
- 紧凑席位将玩家资料、身份/隐藏手牌和公开建筑放入独立格位。4–8 人的 8 张对手建筑会按数量逐级增加同排堆叠，最窄格位仍保留约 7px 可见牌条，建筑不会反向越界或穿入隐藏手牌、身份牌和相邻座位。
- 本人建筑、行动区和无横向滚动的手牌按剩余高度重排。身份选择、刺客和盗贼选择保持单排；说明在左、身份牌在中、确认/取消在右，且最窄尺寸仍保留至少 8px 分栏间距。
- 日志和聊天在紧凑模式下改为居中抽屉，支持遮罩、关闭按钮、Esc、点击外部关闭、焦点约束和关闭后焦点恢复，不再占据或压住牌桌。
- 卡牌悬浮检视仍使用牌桌根节点唯一的固定定位检视器，并受真实视口边缘约束。项目模块归类没有变化，因此未修改结构规范。

### 验收结果

- 身份叫号专项在 `768×600`、`778×638`、`1024×640`、`1262×827`、`1365×668`、`1893×881` 全部通过，覆盖叫号、翻牌、无人应答、座位光圈、持续行动状态、减少动态效果和重连恢复：`output/regression/2026-07-13T16-49-58-129Z/`。
- 身份选择浮窗在上述六种视口通过，三身份与八身份均为单排、无横向浏览，倒计时、标题间距及悬浮检视均通过：`output/regression/2026-07-13T16-53-31-219Z/`。
- 4、5、6、7、8 人在上述六种视口的普通城市和每名对手 8 张建筑矩形碰撞专项全部通过：`output/regression/2026-07-13T17-03-25-107Z/`。
- 4 人真实牌池 `17/16/16/16` 极端手牌和 8 人布局专项通过，所有手牌无滑条，首张、中间张和末张均保留真实指针目标：`output/regression/2026-07-13T16-23-41-993Z/`。
- `768×600` 下刺客单排目标与跳过演出通过：`output/regression/2026-07-13T17-09-45-940Z/`；盗贼单排目标、标记与偷窃结算通过：`output/regression/2026-07-13T17-11-06-160Z/`。
- 九职业收入、保护、建筑师加成、王后相邻收益和减少动态效果专项在紧凑视口通过：`output/regression/2026-07-13T17-18-46-159Z/`。
- 服务端规则测试 109/109 通过，包含 4–8 人明确叫号顺序、未来身份隐藏、旧版本行动快照迁移以及 125 局固定种子全局模拟；客户端、服务端和共享模块生产构建通过。

### 仍然有效的边界

- 最低正式支持尺寸为 `768×600`；更小窗口会提示扩大窗口，但不承诺完整牌桌可读性。
- 正式美术仍为 0/76，是当前唯一已知的 `1.0.0` 发布阻塞；浏览器报告的 `releaseReady=false` 仍仅由此导致。
- 本轮没有改变职业技能数值、建造裁定、胜负规则、客户端 Socket 指令或 V4 调音架构。

## 16. 2026-07-14 皇冠与身份演出节奏、实时计分信息优化

本节覆盖第 14、15 节中的皇冠与身份叫号时长；后续继续处理开场、叫号或计分展示时，以本节为准。

### 已完成实现

- 开场服务端权威时序延长为约 9 秒：0–3 秒展示本局目标，3–7.5 秒皇冠固定在桌面中央并每 600ms 切换一次座位光圈，7.5–9 秒只进行一次最终落位。8 人局会在轮转阶段完整经过全部 8 个座位；刷新和重连按剩余时间恢复，减少动态效果直接在最终座位淡入皇冠，不播放轮转与飞行。
- 身份叫号改为叫号 800ms、无人应答 850ms、身份揭示 1600ms。3D 翻牌使用约 900ms，正面保留剩余约 700ms；无人应答增加 320ms 灰化和盖章入场。刺客跳过时长保持不变，行动倒计时和机器人行动仍只在揭示完成后开始。
- 共享模块新增 `calculateCityScore`、五色集合和奖励常量。实时分数与服务端最终 `scoreGame` 现在复用同一个纯函数：建筑牌分数求和、五色 +3、鬼城补一种缺色、首位完城 +4、其他完城 +2；最终排序仍是总分优先、建筑分次之，没有新增 Socket 或可见状态字段。
- 所有玩家名片常驻显示金币、手牌、`建筑 N/目标` 和星形“当前总分”。自己的建筑区标题固定显示“已建 N/目标 · 建筑分 X · 当前总分 Y”，零建筑时也不会隐藏。
- 左上房间卡原有重复 `?` 入口改为可见的“计分”按钮，右上公共帮助入口保持不变。计分总览逐人显示建筑进度、建筑分、五色进度、完城奖励和当前总分，并固定展示四条奖励规则；支持 Esc、点击遮罩关闭、焦点约束和关闭后恢复焦点。
- 计分总览在 `768×600` 使用居中紧凑抽屉，内容超出时只滚动抽屉内部，不改变牌桌尺寸或产生页面滚动。帮助规则补齐了建筑分、五色、鬼城、首位/其他完城、结束时机和同分排序。
- 本轮仍归入 Match / gameplay、Shared rules 和 Server-only game rules 既有模块，没有改变项目结构，因此未修改 `docs/project-structure-rules.md`。

### 验收结果

- 皇冠专项覆盖 4 人与 8 人局，在 `1893×881`、`1365×668`、`1262×827` 验证 9 秒时序、600ms 光圈、8 人完整轮转、单次落位、刷新/重连和减少动态效果全部通过：`output/regression/2026-07-14T02-24-18-249Z/`。
- 身份叫号专项在 `768×600`、`778×638`、`1024×640`、`1262×827`、`1365×668`、`1893×881` 全部通过，包含 900ms 翻牌、320ms 无人应答入场、揭示座位、行动状态、重连和减少动态效果：`output/regression/2026-07-14T02-23-20-106Z/`。
- 实时计分专项对 4、5、6、7、8 人逐一覆盖上述六种视口，共 30 组组合全部通过。名片四项资源、全员分数拆解、五色与鬼城、首位/其他完城、视口边界、页面滚动、Esc/遮罩关闭和焦点恢复均通过：`output/regression/2026-07-14T02-20-17-898Z/`。
- 服务端规则测试 114/114 通过，其中新增共享计分的普通建筑分、五色、鬼城、首位/其他完城以及叫号时长覆盖，并继续包含 4–8 人共 125 局固定种子模拟。客户端、服务端和共享模块生产构建全部通过。

### 仍然有效的边界

- 正式美术仍为 0/76，是当前唯一已知的 `1.0.0` 发布阻塞；浏览器报告中的 `releaseReady=false` 仍仅由此导致。
- 本轮没有调整奖励数值、建造规则、胜负条件、职业规则、Socket 指令或 V4 调音架构。

## 17. 2026-07-14 牌桌计分信息减负与左上角碰撞修正

本节覆盖第 16 节中的牌桌常驻计分展示；共享计分规则、计分总览和最终结算仍以第 16 节为准。

### 已完成实现

- 左上房间卡改为稳定的两列三行网格：房间号独占首行，阶段文字与无星形的“计分”文字按钮位于第二行，本局目标独占第三行。计分按钮回到普通布局流，不再使用绝对定位；目标在空间不足时省略显示，不会穿入按钮或阶段文字。
- 玩家名片恢复为金币、手牌、建筑数量三项资源。建筑只显示单个数量 `N`，不再显示 `N/目标`；星形图标和牌桌常驻“当前总分”已完全移除。
- 自己的建筑区恢复旧版计数：有建筑时只显示“已建建筑 N”，零建筑时隐藏该行；牌桌上不再常驻目标数量、建筑分或当前总分。
- 计分总览继续保留完整的 `N/目标`、建筑分、五色进度、完城奖励和当前总分；`calculateCityScore` 与服务端最终结算没有回退，也没有改变奖励数值或排序规则。
- 上轮漏检原因已明确：旧专项只确认计分按钮位于视口和房间卡范围内，没有测量房间号、阶段、目标与按钮之间的内部矩形关系。浏览器验收现已为四个元素逐对检查相交，并额外要求阶段与计分按钮至少保留 6px 间距；失败信息会记录元素名称、双方矩形、实际间距和截图。
- 本轮仍属于 Match / gameplay 既有模块，未改变项目结构，因此未修改 `docs/project-structure-rules.md`。

### 验收结果

- 计分专项对 4、5、6、7、8 人逐一覆盖 `768×600`、`778×638`、`1024×640`、`1262×827`、`1365×668`、`1893×881`，共 30 组组合全部通过。左上房间卡内部、相邻名片、每张名片三项资源、建筑单数、零/非零本人建筑计数，以及计分总览的数值、内部滚动、Esc、遮罩关闭和焦点恢复均通过：`output/regression/2026-07-14T02-56-54-816Z/`。
- 身份选择阶段的房间卡内部碰撞和既有职业牌单排专项通过：`output/regression/2026-07-14T02-59-58-982Z/`。
- 身份叫号阶段的长短阶段文字、房间卡内部碰撞和既有叫号演出专项通过：`output/regression/2026-07-14T03-00-50-285Z/`。
- 皇冠开场阶段的房间卡内部碰撞和既有 9 秒开场演出专项通过：`output/regression/2026-07-14T03-01-49-870Z/`。
- 服务端规则测试 114/114 通过，继续包含共享计分与 4–8 人共 125 局固定种子模拟；客户端、服务端和共享模块生产构建全部通过；`git diff --check` 通过，仅有既有 LF/CRLF 转换提示。

### 仍然有效的边界

- 正式美术仍为 0/76，是当前唯一已知的 `1.0.0` 发布阻塞；浏览器报告中的 `releaseReady=false` 仍仅由此导致。
- 本轮没有改变共享计分规则、Socket 协议、服务端结算、建造规则、胜负条件、职业规则或 V4 调音架构。
